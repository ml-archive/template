import Sugar
import Vapor

/// Called after your application has initialized.
public func boot(_ app: Application) throws {
    // MARK: Leaf tags
    // Add your tags like this:
    // let tags = try app.make(MutableLeafTagConfig.self)
    // tags.use(
    //     MyLeafTag()
    // )
}
